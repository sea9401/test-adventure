# 토스페이먼츠 홈페이지 심사 준비

이 문서는 무슨 코인 상품의 토스페이먼츠 홈페이지 심사를 준비하는 절차다. 심사 준비와
실제 유료 판매 출시는 별개다. 사용자의 명시적인 배포 지시 전에는 어떤 환경에도 적용하지
않으며, 테스트 키를 쓰는 심사 기간에도 일반 이용자의 코인 상점은 닫아 둔다.

## 2026-09-21 진행 체크포인트 — 재개 시 여기부터 확인

이 절이 최신 상태다. 사용자는 토스의 결제경로 제출 요청 후 준비 작업의 진행을
지시했다. 이어 심사용 운영 배포와 완료 후 점검 모드 해제를 명시적으로 승인했다.
정확한 main SHA의 CI 및 산출물을 기다린 뒤 배포하는 절차를 진행 중이다.

### 이번 확인 결과

- 통신판매업 신고번호 발급을 신청했다고 사용자가 확인했다. 번호 발급은 대기 중이다.
- 테스트 키 파일은 사용자가 전달한
  `/mnt/c/Users/sea94/OneDrive/바탕 화면/toss-test-keys.txt`다. 환경변수 이름 없이
  `test_gck_`와 `test_gsk_` 형식 키가 각각 하나씩 들어 있다. 값은 출력·Git 저장하지 않는다.
- 테스트 시크릿 키로 존재하지 않는 주문을 조회해 `404 NOT_FOUND_PAYMENT`를 받았다.
  인증은 통과했으며 결제 생성·승인·취소는 실행하지 않았다.
- 로컬 AWS CLI의 로그인 세션이 만료되어 직접 SSM 조회가 실패했다. 기존 SSH 접속과
  EC2 인스턴스 역할을 이용한 SSM 읽기는 성공했다. 로컬 AWS 재인증은 사용자 인증코드 입력을 기다리고 있다.
- 서버의 추적 `.env.production`과 SSM 런타임을 함께 대조했다. 상점 공개 `false`,
  홈페이지 심사 모드 `false`, 결제 `disabled`, 토스 키 미등록 상태다. 사업자명·주소·연락처와
  심사 계정 허용 목록은 기존 기본 설정에 있다. SSM 원본과 런타임 캐시는 일치했다.
- 서버 메모리에서만 테스트 키와 심사 설정을 합쳐 운영 사전검사를 실행했고 통과했다.
  필요한 공개 사업자 정보와 심사 사용자 ID 목록도 SSM에 포함하면 예상 크기는 2,016바이트다.
  실제 SSM·런타임 파일·DB·서비스는 변경하지 않았다.
- `gcrb-review-01` 계정은 존재하며 활성·비제재·프로필 준비 상태이고, 프록시의 코인 상점
  허용 목록에 포함됨을 DB 읽기 전용 트랜잭션으로 확인했다. 이번 작업에서는 로그인 세션을
  생성하거나 비밀번호·세션을 변경하지 않았다.

### 발견한 결제창 차단 원인과 로컬 수정

- 운영 CSP의 `script-src`에 `https://js.tosspayments.com`이 없어, 설치된 Toss SDK가
  사용하는 `https://js.tosspayments.com/v2/standard` 로드가 실제 브라우저에서 차단됐다.
- `next.config.ts`에 정확한 스크립트 출처만 추가했다. CSP 전체 해제나 결제 상점 공개는 없다.
- 실제 `headers()` 결과의 `script-src`를 검사하는 회귀 테스트를 추가해 수정 전 실패와
  수정 후 통과를 확인했다.
- 로컬 브라우저의 해당 페이지 응답에만 수정 CSP를 적용한 뒤 제공된 테스트 클라이언트
  키로 SDK 결제창이 열림을 확인했다. BC·농협을 포함한 카드 선택 UI가 표시됐다.
  이 화면은 운영 코인 상점에서 주문을 생성한 결과가 아니므로 제출용 캡처로 사용하지 않는다.
- 클라이언트 키의 결제창 표시와 시크릿 키 인증을 각각 확인한 상태다. 실제 주문의
  인증·승인을 연결하는 두 키의 종단 간 검증은 아직 하지 않았다.

### 검증과 제출 자료

- 운영 main 기준 배포 브랜치에서 결제·보안·운영 사전검사 6개 파일, 49개 테스트 통과.
  아래 기존 작업 브랜치의 점검 문구 불일치는 이 배포 브랜치에서 재현되지 않았다.
  수정 파일 ESLint와 비밀값 검사도 통과했다.

- 결제·심사 사전검사 관련 6개 파일, 25개 테스트 통과. 사전검사 테스트의 하위 Node 실행은
  샌드박스에서 `EPERM`이 발생해 실행 권한 밖에서 같은 검사를 통과시켰다.
- 수정 파일 ESLint와 `git diff --check` 통과.
- 전체 TypeScript 검사 통과 (`NODE_OPTIONS=--max-old-space-size=6144 npx tsc --noEmit
  --incremental false --pretty false`). 기본 힙 한도에서는 메모리가 부족했다.
- 추가 보안 검사 `src/productionSecuritySurface.test.ts`는 21개 통과·1개 실패했다.
  기존 작업 브랜치에서 변경하지 않은 테스트가 9월 6일 점검 문구를 요구하지만 `deploy/maintenance.html`은
  9월 10일 안내여서 발생한 불일치다. 배포는 운영 main 기준으로 이번 수정만 분리하여 검증한다.
- 공개 상품·약관·로그인 URL은 실제 브라우저에서 HTTP 200을 확인했다. 캡처와 준비 파일은
  `/tmp/toss-review-20260921/`에 있다. 임시 디렉터리는 이후 없어질 수 있다.
- `무슨게임_토스_결제경로_검토용초안.pptx`는 11장이다. 공개 화면과 미완료 항목을 담았으며,
  로그인 후 구매·BC·농협 인증·코인 사용처 화면은 배포 후 실제 캡처로 교체해야 한다.
  로컬 SDK 검증 화면에는 제출용이 아님을 명시했다. 제출 계정 비밀번호는 초안에 넣지 않았다.
- `토스_추가회신_초안.txt`는 PPT 및 실제 심사용 화면이 완성된 후 사용할 문안이다.
  이메일은 발송하지 않았다.

### 남은 확인 사항

1. 사용자가 등록증상 대표자명 `이동훈`을 확인했다. 해당 이름으로 소명서 정정본을
   바탕화면에 전달했고 사용자가 정정본으로 재신청했음을 확인했다. 신고번호는 발급 대기 중이다.
2. 원결제 수단 환불·회원 간 코인 양도 금지의 명시와 사용 및 취소/환불 기간을 약관에
   보완해야 한다. 기간 기준에 대한 토스 답변은 아직 없으므로 임의로 확정하지 않는다.
3. 운영 SSM을 수정할 관리자 자격은 갱신이 필요하다. EC2 역할은 읽기 전용이므로 쓰기
   권한을 임의로 확장하지 않는다.
4. 승인받은 CSP 수정과 테스트 설정을 정규 main/CI/산출물 절차로 적용한다.
   일반 이용자 상점은 닫아 두며, 배포 검증 후 사용자 지시에 따라 점검 모드를 해제한다.
5. 실제 화면을 재확인한 뒤 PDF 가이드에 맞는 URL 포함 캡처·최종 PPT를 만들고,
   발급된 통신판매업 신고번호와 확정된 정책을 반영한다.

## 2026-09-17 진행 체크포인트 (과거 기록)

아래 9월 4일 체크포인트는 과거 기록이며, 계약심사 요청 수신과
1차 회신은 이미 완료됐다. 날짜와 시간은 한국 시간 기준이다.

### 완료 및 확인된 내용

- 9월 16일 20:09 토스 계약 담당자에게 계약심사 필수 질문과 홈페이지·결제경로 자료
  제출 요청을 받음. 대상 상점은 무슨게임, MID `msmsge4l8p`, 신청 결제수단은
  메일 표기상 `복합결제 | 신용카드`.
- 9월 17일 02:21 사용자가 계약심사 1차 답변을 이메일로 회신함. 상품·환불 URL,
  상품 구성(1,000/2,000/3,000/5,000코인, 10,000/20,000/30,000/50,000원),
  계정 귀속·환전 및 계정 간 이전 불가, 단건 결제, 심사 계정, 웹 전용 서비스,
  SDK/API 직접 연동 여부를 전달함. 결제 화면 최종 점검과 PPT는 추가 제출하기로 함.
- 최대 서비스 제공기간을 코인 지급 완료 시점과 코인 사용 가능 기간 중 어느 기준으로
  기재해야 하는지 토스에 문의한 상태. 답변 수신 기록은 아직 없음.
- 9월 17일 새벽 작업에서 심사 계정 `gcrb-review-01` 비밀번호 재설정과 기존 세션
  초기화 후 운영 사이트 로그인 성공을 확인함. 로그인 직후 오류 화면이 있었으나
  세션 준비 후 재접속하자 코인 상점 접근 성공. 상품·환불 안내·사업자정보도 확인함.
- 당시 운영 결제는 `disabled`, 토스 테스트 키는 미등록 상태였음. 이후 이 대화에서
  키 등록이나 심사용 배포는 수행하지 않음.
- 사용자가 개발자센터에서 필요한 `test_gck_` 클라이언트 키와 `test_gsk_` 시크릿 키를
  찾았다고 확인함. 키를 저장한 로컬 파일 경로는 아직 전달받지 않음.
  `test_sk_`는 API 개별 연동 키이며 현재 `widgets()` 연동에 필요한 키 세트와 다름.
- **사용자가 중랑구 관할 통신판매업 신고를 신청했다고 확인함.** 신고번호 발급·처리
  완료 여부와 실제 첨부한 서류는 아직 확인하지 않음. 재개 시 신청부터 다시 안내하지 않음.

### PDF와 작업 파일

- 원본 가이드가 남아 있는 경로:
  `/mnt/c/Users/sea94/OneDrive/바탕 화면/토스페이먼츠_홈페이지_결제경로_제작_가이드_충전업종용.pdf`
- 새벽 대화의 PDF 검토 기록: 모든 캡처에 URL 표시, BC·농협 카드의 결제 직전 인증
  화면, 코인으로 게임 내 상품을 구매하는 경로가 필요함. 테스트 결제창으로 심사가
  가능하며 결제 완료 화면은 필수 제출 항목이 아니라고 확인함.
- 같은 검토에서 원결제 수단 환불·양도 금지·사용 및 취소/환불 가능 기간에 대한 약관
  확인과 보완, 통신판매업 신고번호 표시를 후속 항목으로 정리함.
- 당시 `/tmp/toss-review-20260917/readiness.md`와 캡처를 만들었으나, 재개 후 해당
  디렉터리가 없는 것을 확인함. PPT 완성본은 확인되지 않았으며 캡처를 다시 만들어야 함.
- 새벽 대화 기록은
  `/home/sea9401/.codex/sessions/2026/09/17/rollout-2026-09-17T02-09-39-01a0ab32-0cc9-7331-9a50-c4438f0a0449.jsonl`
  에서 확인했음. 기록에 인증정보가 포함될 수 있으므로 원문을 문서나 출력에 복사하지 않음.

### 다음 작업

1. 통신판매업 신고 처리 결과·보완 요청과 발급된 신고번호를 확인한다. 구매안전서비스
   비적용 확인서 제출 방향을 안내했으나 중랑구 지정 양식·요구 증빙은 확인하지 못했음.
   실제 추가 제출은 관할 담당자의 안내를 따른다.
2. 같은 세트의 테스트 키를 안전하게 전달받을 로컬 파일 위치를 확인하고 운영 설정을
   준비한다. 키 값과 비밀번호는 Git·이 문서에 저장하지 않는다.
3. 원본 PDF, 현재 약관·상품 페이지를 대조하고 제공기간에 대한 토스 회신을 반영한다.
   서비스 기간을 임의로 확정하지 않는다. 발급된 통신판매업 신고번호 표시도 준비한다.
4. **명시적인 심사용 배포 요청을 받은 뒤에만** 테스트 결제를 활성화해 배포한다.
   일반 이용자 상점은 닫아 두고 심사 계정으로 결제창을 확인한다.
5. 가이드에 맞춰 화면을 다시 캡처하고 결제경로 PPT를 제작한다. 완성한 자료와
   심사용 화면 준비 결과로 추가 회신 초안을 작성한다. 이메일 발송은 별도 지시가 필요하다.

## 2026-09-04 진행 체크포인트 (과거 기록)

완료된 항목:

- 공개 무슨 코인 상품 페이지와 네 상품별 고유 이미지 구성
- 대문·상품 페이지 사업자 정보 표시 구성
- 대표자명, 사업장 주소, 공개 연락처를 운영 공개 설정에 반영
- Toss Payments SDK v2 결제창 이벤트 흐름과 결제·환불 서버 기능 구현
- 같은 MID에서 발급된 `test_gck_`·`test_gsk_` 테스트 키 세트 보유 확인
- 토스페이먼츠에 계정 귀속형 게임 재화의 심사용 계정 허용 여부 문의 이메일 발송
- 토스페이먼츠 회신으로 회원 전용 결제 화면은 입점·홈페이지 심사 때 요청받은 테스트
  계정을 제출하는 방식임을 확인
- 운영 DB 읽기 전용 점검으로 `gcrb-review-01`~`03` 계정이 모두 존재하고 비활성화·제재
  상태가 아님을 확인 (`01`·`02`는 프로필 준비됨, `03`은 프로필 미설정)
- 운영 SSM에 기존 `REVIEW_LOGIN_*` 세 값이 함께 설정되어 있음을 값 비노출로 확인
- 토스 제출 계정으로 `gcrb-review-01`을 선택하고 새 scrypt 비밀번호 해시로 교체한 뒤 기존
  활성 세션을 초기화함 (평문 비밀번호는 출력·저장하지 않고 사용자 클립보드에만 전달)
- 운영 로그인 화면에서 `gcrb-review-01`의 새 비밀번호 로그인이 실제로 성공함을 확인
- 토스 계약 가입비 결제를 완료하고 심사 결과 대기 상태임을 확인

재개할 때 확인할 항목:

1. 토스의 심사 결과와 추가 서류·홈페이지 심사 정보 요청을 확인한다. 게임 내 충전성 재화는
   유의 업종이라 최종 입점 가능 여부는 계약 담당부서의 실제 심사에서 결정된다.
2. 홈페이지 심사 요청을 받으면 같은 세트의 Toss 테스트 키를 Git이 아닌 운영 SSM
   SecureString에 등록한다.
3. 사용자가 명시적으로 심사용 배포를 요청한 뒤에만 홈페이지 심사 모드와 테스트 결제를
   활성화해 배포한다.
4. 선택한 심사 계정의 기존 활성 세션을 제출 직전에 비우고, 외부에서 상품·사업자 정보·정책
   링크·로그인·테스트 결제창을 확인한 뒤 심사 URL과 계정을 안전한 채널로 전달한다.

현재 배포와 키 등록은 수행하지 않았다. 운영 기본값은 상점 비공개, 결제 비활성,
홈페이지 심사 모드 비활성이다. 실제 테스트 키와 심사용 비밀번호는 이 문서에 저장하지 않는다.

운영 SSM의 기존 `REVIEW_LOGIN_*`은 카카오 계정에 연결하는 호환 경로이며, 2026-09-04 점검
기준 해당 대상은 코인 상점 허용 목록과 일치하지 않았다. 토스에는 코인 상점 접근이 이미
허용된 별도 `password_credentials` 계정 `gcrb-review-01` 또는 `gcrb-review-02`를 제출한다.
현재 제출 대상으로 정한 `01`은 비밀번호를 교체하면서 `active_session_id`를 비웠다. 대체 계정
`02`에는 기존 활성 세션이 있으므로 실제 제출 대상으로 바꿀 때만 해당 세션을 비운다.

## 현재 공개 표면

- 대문: `/sign-in`
- 판매 상품: `/products/museun-coin`
- 이용약관과 환불 기준: `/terms`
- 개인정보처리방침: `/privacy`
- 실제 구매 화면: `/settings/coin-shop` (로그인한 심사 계정만 접근)

판매 상품 화면에는 1,000·2,000·3,000·5,000 코인 상품, 원화 가격, 서로 다른 상품 이미지,
결제 승인 후 즉시 지급, 계정 귀속과 환전 금지, 환불 기준 링크를 표시한다. 대문과 상품
화면에는 상호와 사업자등록번호를 표시하며, 아래 환경 변수까지 입력하면 대표자·주소·연락처도
함께 표시한다.

## 아직 받아야 할 실제 값

사업자등록증과 신청서에 적은 내용 그대로 다음 값을 준비한다. 임의 값이나 예시 값으로
심사를 요청하지 않는다.

| 환경 변수 | 입력할 내용 | 공개 여부 |
| --- | --- | --- |
| `PUBLIC_MERCHANT_REPRESENTATIVE` | 사업자등록증의 대표자명 | 공개 |
| `PUBLIC_MERCHANT_ADDRESS` | 사업자등록증의 사업장 주소 | 공개 |
| `PUBLIC_MERCHANT_CONTACT` | 구매자 문의를 받을 전화번호 | 공개 |
| `PUBLIC_MERCHANT_MAIL_ORDER_SALES_NUMBER` | 발급된 경우 통신판매업 신고번호 | 공개·선택 |

상호 `무슨게임`과 사업자등록번호 `781-52-01091`은 공개 상수로 관리한다. 실제 값이 달라지면
심사 배포 전에 코드와 등록증을 대조해 수정한다.

## 심사 전 비밀 설정

비밀값은 Git, 문서, 채팅, 브라우저 코드에 넣지 않고 운영 비밀 저장소에만 등록한다.

```dotenv
NEXT_PUBLIC_MUSEUN_COIN_SHOP_OPEN=false
PG_HOMEPAGE_REVIEW_MODE=true
MUSEUN_COIN_PAYMENTS_MODE=test
TOSS_PAYMENTS_CLIENT_KEY=<test_gck로 시작하는 결제 클라이언트 키>
TOSS_PAYMENTS_SECRET_KEY=<같은 세트의 test_gsk 시크릿 키>

REVIEW_LOGIN_ID=<심사자용 로그인 ID>
REVIEW_LOGIN_PASSWORD=<12자 이상 임시 비밀번호>
REVIEW_LOGIN_USER_EMAIL=<기존 심사 계정 이메일>
MUSEUN_COIN_SHOP_REVIEW_USER_IDS=<해당 계정의 users.id>
```

클라이언트 키와 시크릿 키는 반드시 같은 상점·같은 세트의 테스트 키를 쓴다. 시크릿 키에는
`NEXT_PUBLIC_` 접두사를 붙이지 않는다. 심사 계정은 실제 구매 화면에만 필요하며 공개 상품과
정책 화면은 로그인 없이 열려야 한다. 계정 귀속형 게임 재화라 비회원에게 지급할 수 없다는
점과 심사 계정을 토스 신청 메모 또는 심사 담당자에게 안전한 채널로 전달한다.

## 배포 직전 점검

1. 사업자 정보가 사업자등록증과 일치하는지 확인한다.
2. `/products/museun-coin`에서 네 상품 이미지가 모두 보이고 깨진 이미지나 빈 목록이 없는지 확인한다.
3. 대문 푸터에서 상호·등록번호·대표자·주소·연락처를 확인한다.
4. 심사 계정으로 로그인한 뒤 `/settings/coin-shop`에 접근되는지 확인한다.
5. `node scripts/check-production-env.mjs`가 전체 운영 환경에서 통과하는지 확인한다.
6. 결제창에서 신청한 카드·간편결제 수단이 보이는지 확인한다.

일반 운영 배포 절차를 사용한다. 정확한 `main` SHA의 CI와 운영 산출물이 준비된 뒤 런타임
교체 직전에만 배포 워크플로가 점검 모드를 켜게 하며, 배포가 끝났다고 점검 모드를 자동으로
해제하지 않는다.

## 테스트 결제 확인

1. 각 상품 중 하나로 결제창을 열고 주문명과 금액을 확인한다.
2. 결제창을 닫은 뒤 버튼이 다시 활성화되는지 확인한다.
3. 테스트 결제를 완료해 성공 URL로 이동하고 코인이 한 번만 지급되는지 확인한다.
4. 새로고침과 성공 URL 재호출로 중복 지급되지 않는지 확인한다.
5. 실패 결제로 실패 URL과 주문 상태를 확인한다.
6. 결제 내역에서 미사용 코인 환불 요청과 운영자 환불 검토 흐름을 확인한다.
7. 토스 개발자센터의 테스트 결제 내역과 내부 주문번호·금액·상태를 대조한다.

실제 카드 대금이 움직이는 라이브 키로 홈페이지 심사를 시험하지 않는다.

## 심사 종료 후

실제 출시를 별도로 승인받기 전까지 다음 상태로 되돌린다.

```dotenv
PG_HOMEPAGE_REVIEW_MODE=false
NEXT_PUBLIC_MUSEUN_COIN_SHOP_OPEN=false
MUSEUN_COIN_PAYMENTS_MODE=disabled
```

심사 계정 비밀번호는 회전하거나 비활성화하고 더 이상 필요 없는 테스트 시크릿은 운영 비밀
저장소에서 제거한다. 공개 상품 페이지와 사업자 고지는 유지할 수 있다. 라이브 결제 전환은
카드사 심사 완료, 약관 시행일 공지, 웹훅, 환불·장애 대응 확인을 마친 뒤 별도 변경과 별도
배포로 진행한다.
