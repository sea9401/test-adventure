# 미배포 6개 항목 스쿼시 계획

> 실행: `superpowers:executing-plans`를 적용한다. AGENTS.md에 따라 추가 승인이나 서브에이전트 없이 현재 세션에서 실행한다.

**Goal:** 사용자가 확인한 미배포 게임 변경 6개를 하나의 로컬 커밋으로 통합한다.

**Architecture:** 운영 기준 `f262d8f79df8e7f3097564411ed73cf7805ec556` 위에 대상 변경만 적용한다. 현재 개발 브랜치는 운영보다 오래된 기반이므로 전체 병합하지 않고 원본과 백업을 보존한다.

**Tech Stack:** Git, Next.js 16.3.3, TypeScript, Vitest.

## 범위와 출처

| 항목 | 원본 커밋 |
| --- | --- |
| 목장 일괄 사료 채우기에 돼지 입식 포함 | `2f7e1beac` |
| 거래소 관심 매물 계정 저장·동기화·기존 목록 가져오기 | `7453f3adc` |
| 정신·MP 성장 툴팁과 안내서 정정 | `7453f3adc` |
| 비천무신 낙성·파공 상향 및 11 SP 유지 | `8c3f687d3` |
| 결투가 패턴 편집에서 저장한 하위 선언 보존 | `9a8dbf0e6` |
| 문장 종류·등급·획득순 정렬과 장착 슬롯 표시 | `481c709aa`, `4c1741a68` |

## 제약

- 결과 브랜치: `release/six-updates-20260919`.
- 원본 백업: `backup/pre-six-updates-squash-20260919` (`9a8dbf0e6`). 문장 원본 브랜치도 보존한다.
- 서버 증설·토스 심사 문서는 이 통합 범위에서 제외하고 원본에 보존한다.
- 운영 기준의 다른 기능·의존성·배포 설정을 유지한다.
- 푸시, PR, main 병합, 배포, 운영 데이터 변경은 포함하지 않는다.
- 기존 구현과 회귀 테스트를 통합하는 작업이므로 신규 기능용 TDD는 반복하지 않는다.

## 실행

- [x] 작업 트리 청결, 원본 커밋, 운영 기준을 확인하고 `/tmp`에 격리 작업 공간을 생성한다.
- [x] 운영 잠금 파일로 의존성을 설치하고 대상 기존 테스트의 기준 결과를 확인한다.
- [x] `git cherry-pick --no-commit 2f7e1beac 7453f3adc 8c3f687d3 9a8dbf0e6 481c709aa 4c1741a68`로 변경을 적용한다. 충돌 없이 적용되었다.
- [x] 관련 Vitest, 전체 TypeScript, 변경 파일 ESLint, 모듈 크기와 diff 공백 검사를 실행한다.
- [x] 최종 diff를 자체 검토하고 원본 변경 파일의 누락·추가 여부를 검사한다.

최종 저장은 위 6개 항목과 이 기록을 하나의 커밋으로 만든 뒤, 기준 대비 커밋 수
1개·깨끗한 작업 트리·원본 브랜치 보존 여부를 확인하는 순서로 진행한다.

## 검증 결과

- 운영 잠금 파일로 `npm ci --ignore-scripts --no-audit --no-fund` 설치 성공. 잠금 파일 변경 없음.
- 통합 전 기준 테스트: 목장·관심 매물·비천무신·문장·안내서 6개 파일, 97개 테스트 통과.
- 통합 후 관련 Vitest: 194개 파일, 2,180개 테스트 통과. 전투 디렉터리 전체와 거래소 화면·API, 목장, 문장·레벨 성장, 패턴 편집·저장, MP 성장, 스킬 데이터·장착 화면, 안내서를 검사했다.
- 전체 TypeScript 검사: `node --max-old-space-size=8192 node_modules/typescript/bin/tsc --noEmit --incremental false` 통과.
- 변경 TypeScript 28개 파일 ESLint, 모듈 크기 검사 64개 파일, `git diff --cached --check` 통과.
- 원본 6개 커밋에서 바뀐 35개 파일과 통합 변경 경로가 정확히 일치했다. 이 스쿼시 기록만 별도로 추가한다.
- 자동 병합된 8개 파일을 포함한 diff 자체 검토 완료. 운영의 직업·문장 기반과 최신 스킬·안내서 변경을 보존했고 통합을 위한 코드 수정은 필요하지 않았다.
- 전체 프로젝트 테스트·실제 DB·브라우저 검증·운영 CI·배포는 이번 스쿼시 검증에 포함하지 않았다.
