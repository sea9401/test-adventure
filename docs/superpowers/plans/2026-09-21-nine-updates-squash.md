# 미배포 게임 변경 9건 스쿼시

> 실행: `superpowers:executing-plans`. 사용자 지시에 따라 추가 승인·서브에이전트 없이 순서대로 실행한다.

**Goal:** 확인된 미배포 게임 변경 9건을 최신 main 위의 한 개 로컬 커밋으로 통합한다.
**Architecture:** 토스 수정이 포함된 `b9a011b2682691812b3b19ec7769b426a63f4f90`을 기준으로 아래 원본 커밋의 변경만 적용한다. 기존 개발 브랜치와 미개척지 프리셋 브랜치를 보존한다.
**Tech Stack:** Git, Next.js 16.3.3, TypeScript, Vitest, PostgreSQL.

## 범위

| 작업 | 원본 커밋 |
| --- | --- |
| 숙련의 탑 체크포인트 재입장 | `312e1128e` |
| 제작 재료 수급처 바로 이동 | `453d74f94` |
| 완전식 발동 조건 | `f8fff844c` |
| 완료 우편 일괄 삭제 | `d9f8356fd` |
| 사람 확인 대기 복구 | `4e100dba6` |
| 농장 주간 납품 보너스 안내 | `ae47e16ba` |
| 요리 숙련 기록 복구 | `79f1f78db` |
| 길드장 72시간 미접속 승계 | `197d1dc22` |
| 미개척지 독립 프리셋 3개 | `96b9e98f1` |

## 제약

- 결과 브랜치 `release/nine-updates-20260921`, 작업 공간 `/tmp/adventure-pending-nine-20260921`.
- 토스 수정은 main에 이미 포함되어 있으므로 중복 적용하지 않는다.
- 원본 브랜치·커밋과 운영 기본 설정·기존 기능을 보존한다.
- 이번 요청은 로컬 스쿼시다. 새 게임 변경의 푸시·PR·배포를 수행하지 않는다.
- 이미 구현된 변경을 통합하므로 신규 TDD를 반복하지 않고 기존 회귀 테스트로 검증한다.

## 실행 및 검증

- [x] 원본 작업 트리가 깨끗함과 main·원본 커밋을 확인하고 /tmp 작업 공간 생성.
- [x] main 잠금 파일로 의존성을 설치하고 기존 관련 테스트 기준 결과 확인.
- [x] `git cherry-pick --no-commit 312e1128e 453d74f94 f8fff844c d9f8356fd 4e100dba6 ae47e16ba 79f1f78db 197d1dc22 96b9e98f1` 적용. 충돌은 원본 의도와 최신 main 동작을 함께 보존한다.
- [x] 변경된 회귀 테스트와 전투·미개척지 연관 테스트, 길드장 승계 임시 PostgreSQL 통합 테스트 실행.
- [x] 전체 TypeScript, 변경 파일 ESLint, 모듈 크기, 비밀값·공백 검사.
- [x] 원본 변경 경로 대조와 diff 자체 검토로 누락·범위 외 변경 확인.
- [x] 한 개 커밋으로 저장하고 기준 대비 커밋 수·깨끗한 작업 트리·원본 보존 확인.

## 통합 기록

- main 기준 기존 테스트 18개 파일, 299개 통과.
- 완전식 조건 원본의 `engine.ts`/`engine-pvp.ts` 변경은 운영에서 분리된
  `engine.playerSkills.ts`/`engine.pvpSkillInput.ts`로 옮겼다. 전체 엔진 파일은 main과 동일하다.
- 패턴 조건을 스킬별로 평가하면서 기존 `onConditionRejected` 진단 콜백을 보존하고 회귀 테스트를 추가했다.
- 사람 확인·길드 탈퇴 패널은 원본 변경의 공통 불투명 surface 토큰으로 통합했다.

## 검증 결과

- 통합 회귀 검사: 169개 파일, 2,057개 테스트 통과. 전투 디렉터리 전체와 9건의 변경 테스트,
  토스 결제 게이트를 검사했다. 길드장 승계 4개 실제 PostgreSQL 통합 테스트도 포함한다.
- 기존 저장·변경 경로 보존 검사: 미개척지 저장 파서·서비스, 길드 활동 기록, 사람 확인 상태,
  우편 화면 기존 검사 5개 파일, 36개 테스트 통과. 합계 174개 파일, 2,093개 테스트다.
- 전체 TypeScript 검사 통과 (`NODE_OPTIONS=--max-old-space-size=6144 npx tsc --noEmit --incremental false --pretty false`).
- 변경 TypeScript 55개 파일 ESLint, 모듈 크기 검사 64개 파일, 비밀값 검사·자체 검사 통과.
  비밀값 검사의 Git 하위 호출은 샌드박스 EPERM 후 승인된 실행으로 통과했다.
- 원본 71개 경로를 대조했다. 전투 엔진 2개 경로만 현재 분리 모듈 2개로 옮겼으며,
  그 외 원본 변경 경로는 누락 없이 포함했다. 이 통합 기록만 별도 추가한다.
- 충돌 해결 및 권한·트랜잭션·프리셋 저장 호환성을 자체 검토했다. 사용자 지시에 따라
  서브에이전트는 사용하지 않았다.
- 운영 환경 기본값, 의존성, DB 스키마, 배포 워크플로, 토스 CSP 설정은 기준 main과 동일하다.
- 전체 저장소 테스트·브라우저·운영 CI·배포는 이번 로컬 스쿼시 범위에 포함하지 않았다.

- 커밋 후 기준 main 대비 커밋 수 1개와 깨끗한 작업 트리를 확인했다. 원본 두 브랜치의 SHA도 보존됐다. 임시 PostgreSQL은 검증 후 종료했다.
