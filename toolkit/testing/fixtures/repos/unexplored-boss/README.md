# Unexplored boss adapter fixture

The adapter integration test constructs the minimal named TypeScript registries in a temporary workspace.
No fake boss is copied into the real product catalog; the shared `echo_warden` YAML fixture is applied only
inside that disposable repository.
